services:
  osrm:
    image: ghcr.io/project-osrm/osrm-backend:26.8.0-debian
    command: osrm-routed --algorithm mld /data/nwfd-latest.osrm
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ../data/osm:/data:ro
    restart: unless-stopped
